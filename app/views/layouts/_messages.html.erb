<%# Rails flash messages styled for Bootstrap 4.0 %>
<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="application-notice alert alert-<%= name.to_s == 'notice' ? 'success' : 'danger' %>"><%= msg %></div>
  <% end %>
<% end %>
