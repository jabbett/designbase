<p id="notice"><%= notice %></p>

<h1>Transcripts</h1>

<table>
  <thead>
    <tr>
      <th>Participant</th>
      <th>Interviewed at</th>
      <th>Description</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @transcripts.each do |transcript| %>
      <tr>
        <td><%= transcript.participant %></td>
        <td><%= transcript.interviewed_at %></td>
        <td><%= transcript.description %></td>
        <td><%= transcript.content %></td>
        <td><%= link_to 'Show', transcript %></td>
        <td><%= link_to 'Edit', edit_transcript_path(transcript) %></td>
        <td><%= link_to 'Destroy', transcript, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Transcript', new_transcript_path %>
